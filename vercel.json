{
  "buildCommand": "pnpm install --no-frozen-lockfile && pnpm --filter paylink-web build",
  "installCommand": "pnpm install --no-frozen-lockfile",
  "outputDirectory": "apps/web/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}